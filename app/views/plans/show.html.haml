- @title = @plan.title.dup if @plan.present? && @plan.title.present?
- @meta_url = "#{request.protocol}#{request.host_with_port}#{request.fullpath}"

- content_for :additional_meta do

  %meta{property: "og:title",       content: @title }
  %meta{property: "og:url",         content: @meta_url}
  %meta{property: "og:description", content: @plan.about }
  %meta{name: "twitter:card",             content: "summary"}
  %meta{name: "twitter:url",              content: @meta_url}
  %meta{name: "twitter:title" ,           content: @title}
  %meta{name: "twitter:description",      content: @plan.about}
  %meta{name: "twitter:image",            content: "#{url_for(asset_path('Bible-app-icon-english-120x120.png'))}"}
  %meta{name: "twitter:site",             content: "@YouVersion"}

%article.reading_plan_index
  = image_tag @plan.hero_image.url, class: 'rp-hero-img' if @plan.hero_image.present?
  %h1.keep= @plan.name

  .rp-plan-info
    %p
      30 days

  .rp-plan-about
    .rp-plan-about-buttons
      - if logged_in?
        = render partial: "/plans/button", locals: {plan: @plan}
      - else
        = link_to t('plans.subscribe to plan'), sign_up_path(source: "plan", id: @plan.id, redirect: request.fullpath), class: "action_button_green"
        = link_to t("plans.view sample"), {day: 1.to_s}, {class: 'action_button_dark_grey'}

    %p= @plan.about


  .mobile

    %p
      = @plan.copyright.html_safe
    - unless @plan.publisher_url.nil?
      = link_to(t("plans.about publisher"), @plan.publisher_url)
