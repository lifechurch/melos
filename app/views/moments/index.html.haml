- @sidebar = false
- content_for :stylesheets do
  = stylesheet_link_tag "slick.css"

#spinner
.social-feed-wrap{"data-str-load-more" => t("moments.action.load more")}
  :javascript
    $(document).ready(function(){
      var feed = new Moments.Feed()
    });
