:ruby
  # Locals Parameters
  # moment - a single Moment to display  (Bookmark, Highlight, Note)
  # comments_displayed - boolean, true or false (whether or not to display comments when rendering html)

.moment
  -# !the content of the moment
  .moment-top
    .moment-sys
      %span.moment-icon
        &nbsp;
      %span.moment-avatar
        = image_tag moment.avatars.lg_avatar.url
      %span.moment-time
        = api_dt_time_ago(moment.created_dt)
      %h2.moment-sys-text
        = moment.moment_title.html_safe

      - if moment.body_images.present?
        = link_to(moment.body_images.action_url || "#") do
          = image_tag(moment.body_images.select_optimized_width(400).url, width: "414")

      %div{style:"clear:both"}
      - if moment.body_text.present?
        = simple_format(moment.body_text.html_safe)