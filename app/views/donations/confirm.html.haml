%article
  %h1= t('make a donation')
  %p= t('donate.header confirm copy')
  = form_for :sim_transaction, :url => AuthorizeNet::SIM::Transaction::Gateway::TEST do |f|
    = sim_fields(@sim_transaction)
    - @tr.each do |k, v|
      = hidden_field_tag k, v
    = hidden_field_tag :x_header_email_receipt, t('donate.email header')
    = hidden_field_tag :x_footer_email_receipt, t('donate.email footer')
    = hidden_field_tag :x_merchant_email, "giving@lifechurch.tv"

    %p
      %strong= "#{t('donate.name')}: "
      %span= "#{@tr[:x_first_name]} #{@tr[:x_last_name]}"
      %br
      %strong= "#{t('users.email address')}: "
      %span= @tr[:x_email]
    %p
      %strong= "#{t('donate.amount')}: "
      %span= "$#{@tr[:x_amount]}"
      %br
      %strong= "#{t('donate.card number')}: "
      %span=  "XXXX-XXXX-XXXX-#{@tr[:x_card_num][-4, 4]}"
      %br
      %strong= "#{t('donate.card exp date')}: "
      %span=  @tr[:x_exp_date]
      %div
        %button{class: "action_button_green", action: "submit"}
          = t('donate.donate now')


