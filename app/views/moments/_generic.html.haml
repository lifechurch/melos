.moment
  -# !the content of the moment
  .moment-top
    .moment-sys
      %span.moment-time
        = api_dt_time_ago(moment.created_dt)
      %p.moment-sys-text
        = moment.moment_title.html_safe

      - if moment.body_images.present?
        = link_to(moment.body_images.action_url || "#") do
          = image_tag(moment.body_images.select_optimized_width(400).url, width: "414")

      - if moment.body_text.present?
        %p{style: "clear: both;"}= moment.body_text

  -# !share controls, dates, and comments
  .moment-btm
    = render partial: "moments/actions_bar", locals: {moment: moment}