#widget-signup.widget.simple
  .bd
    %h3
      = t("no account")
      - redirect = params[:redirect] || (request.referer ? URI(request.referer).path : nil)
    = link_to t("sign up"), sign_up_path(redirect: redirect, source: params[:source]), class: "action_button_grey"