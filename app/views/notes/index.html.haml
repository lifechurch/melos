- content_for :sidebar do
<<<<<<< HEAD
  / = render_cell :likes_list_widget, :display, likes: @likes, title: "Likes", user_id: @user_id
=======
  = render_cell :likes_list_widget, :display, likes: @likes, title: t("likes.likes"), user_id: @user_id
>>>>>>> I18n underpinnings for all view files except widgets
  = render '/shared/widget_ad_bible_app'

%article
  - if current_auth
    - if request.path == user_notes_path(current_user)
      %h1= t("notes.my notes")
      %dl.combo_button.float_right
        %dt View:
        %dd
          = link_to t("notes.all notes"), notes_path
        %dd.highlighted
          = link_to t("notes.my notes"), user_notes_path(current_user)
    - else
      %h1= t("notes.all notes")
      %dl.combo_button.float_right
        %dt View:
        %dd.highlighted
          = link_to t("notes.all notes"), notes_path
        %dd
          = link_to t("notes.my notes"), user_notes_path(current_user)
  - else
    %h1= t("notes.all notes")
  - if @notes
    %ul.big_list
      - @notes.each do |note|
        %li{id: note.id}
          %h5= link_to note.title, note_path(note.id)
          %p.mute.small.negative_gutter_top_half= link_to_references(note.references) 
          %p= note.content
          %div{class: "status"}= note.user_status
  - else
    %div{class: "none"}= t("notes.none exist blurb")
