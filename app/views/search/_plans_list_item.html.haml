- item = plans_list_item
%li.plan
  %h3
    = link_to YV::Resource.i18nize(item.name), plan_path("#{item.id}-#{item.slug}")
  %span.length
    = YV::Resource.i18nize(item.formatted_length)
  %p
    = YV::Resource.i18nize(item.about)