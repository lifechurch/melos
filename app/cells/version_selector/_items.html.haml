- versions.each do |v|
  %tr{"data-version" => v.id, "data-abbrev" => v.abbreviation}
    %th
      %a.spinner_trigger{:href => "#"}= v.human
    %td{class: v.audio_version? ? "audio" : ""}
      %a.spinner_trigger{:href => "#"}
        %div= v.title