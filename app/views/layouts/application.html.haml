!!! 5
%html{class: (@html_classes.join(' ') if @html_classes)}
  %head{:lang => 'en-us'}
    = csrf_meta_tags
    - if @canonical
      %link{rel: "canonical", href: @canonical}
    = render '/shared/head'
  %body
    #wrapper{:class => @fixed_width == true ? 'fixed_width' : ''}
      #header_outer
        = render '/shared/header'
      - flash.each do |key, msg|
        %div{class: "flash_#{key}"}= msg
      #container{:class => @sidebar == false ? 'no_sidebar' : ''}
        #main{:role => "main", :class => @is_reader == true ? 'main_reader' : ''}
          = render '/shared/nav_primary'
          = content_for?(:main) ? yield(:main) : yield
          - if content_for? :main_trailer
            = yield :main_trailer
        - if content_for? :sidebar
          #sidebar
            = yield :sidebar
        - else
          - unless @sidebar == false
            = render '/shared/sidebar'
      = render '/shared/footer'
    - if @reader_menus == true
      -# = render '/shared/menu_book_chapter'
      = render_cell :book_selector, :display, {reference: @reference, version: @version}
      -# = render '/shared/menu_version'
      = render_cell :version_selector, :display, {reference: @reference, alt_reference: @alt_reference, version: @version}
      = render '/shared/menu_bookmark'
      = render '/shared/menu_settings'
      = render '/shared/menu_audio_player'
      = render '/shared/modal_single_verse'
      = render '/shared/modal_share_verse'

    = render '/shared/javascripts'
    %meta{name: "msApplication-ID", content: "b313456f-e3a2-4985-a376-3633c32f7f39"}
    %meta{name: "msApplication-PackageFamilyName", content: "b313456f-e3a2-4985-a376-3633c32f7f39_54kmqrgfdyhqr"}
